# ui.statusline.modules.lsp

LSP-first breadcrumbs for the NvChad statusline (async + cached), with a
Treesitter fallback when no LSP is attached. Submodules are lazy-loaded to
break circular dependencies.
