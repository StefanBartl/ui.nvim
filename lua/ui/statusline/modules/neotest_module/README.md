# ui.statusline.modules.neotest_module

Statusline segment: neotest's own running/passed/failed counts, colored per
status, empty when nothing has run yet.

**Currently unreferenced.** Only mentioned in passing in sibling modules'
comments ("same highlight convention as neotest_module"), never actually
required by any of the 6 live statusline variants — noted 2026-08-16 while
triaging `unreferenced-module` findings.
